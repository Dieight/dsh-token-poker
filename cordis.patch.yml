# dsh-token-poker — bundle patch layer.
#
# Installing this package into a profile (`dsh plugin --profile <name> add dsh-token-poker`)
# appends it to `dsh.profile.bundles`; this patch is then applied as that
# bundle's layer. The row below loads the package's main entry as the host
# half (a plain Cordis plugin); the web app also loads its `./client` bundle
# as the browser half because the package declares `dsh.client`.
- insert:
    - id: dsh-token-poker
      name: dsh-token-poker
